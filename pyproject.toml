[project]
name = "rtsp-scheduler"
version = "0.1.0"
description = "Schedule RTSP traffic camera recordings with FFmpeg"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.10.0",
    "sv-ttk>=2.6.0",
    "tkcalendar>=1.6.0",
]
